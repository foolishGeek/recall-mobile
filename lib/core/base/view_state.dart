// Recall · ViewState. The async/UI status every controller exposes so views can
// render loading / error without business logic in the widget tree.

enum ViewState { idle, loading, success, error }
