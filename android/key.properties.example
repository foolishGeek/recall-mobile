# Copy to android/key.properties (gitignored) and fill in real values.
# Generate the keystore first (see recall-mobile/README.md → "Release signing").
storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=upload
# Path to the .jks — relative to android/app/, or an absolute path.
storeFile=../upload-keystore.jks
